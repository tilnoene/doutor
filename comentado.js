const getEmoji = require('get-random-emoji');

/*client.on('presenceUpdate', (oldPresence, newPresence) => {
  let member = newPresence.member;
  
  if (member.id === process.env.NATHALIA_ID) {
    if (oldPresence.status !== newPresence.status) {
      let channel = member.guild.channels.cache.get(process.env.CHANNEL_ID);

      if (newPresence.status === 'offline') {
        //sendMessageToUser(':rose: ' + getEmoji());
        /*client.user.setActivity('natalia off :(',
        {
          type: 'STREAMING',
          url: 'https://www.twitch.tv/liaoli'
        });
        //channel.send('O amor da minha vida está offline :(');
      } else {
        /*client.user.setActivity('natalia on :D', {type: 'COMPETING'});
        //channel.send('Aêee!!! O amor da minha vida está online :D');
      }
    }
  }
});*/

/*client.user.setPresence({
          activity: { name: 'Serveur Hors Ligne' },
          status: 'idle',
        });*/
        